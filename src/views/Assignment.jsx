/**
 * Write a component that:
 * - Has two text inputs
 * - If the sum of the two inputs is 42, display text "Done"
 * - Use the following functions: U.atom, U.add, U.ift, U.equals
 * - Extra if time:
 *      - show a warning if input values are non-integers (decimals, not numbers...)
 */

const Assignment = () => {
    return (
        <div>
            <h1>Instructions in src/views/Assignment.jsx</h1>
        </div>
    )
}

export default Assignment
